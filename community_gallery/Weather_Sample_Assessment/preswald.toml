[project]
title = "Preswald Project"
version = "0.1.0"
port = 8501
slug = "my-example-project-510909"  # Required: Unique identifier for your project
entrypoint = "hello.py"    # Required: Main script to run when executing preswald run

[branding]
name = "Preswald Project"
logo = "images/logo.png"
favicon = "images/favicon.ico"
primaryColor = "#F89613"

[data.weather_csv]  # Add this section to load your weather dataset
type = "csv"
path = "data/weather.csv"  # Ensure this is the correct path

[logging]
level = "INFO"  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
format = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"