from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Product Usage Analytics")
text("Track feature adoption, user behavior, and engagement patterns across your product.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
