from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Investor Relations Dashboard")
text("Track key metrics, shareholder communications, and investor meeting materials.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
