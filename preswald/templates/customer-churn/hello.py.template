from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Customer Churn Dashboard")
text("Monitor customer retention, churn rates, and risk factors in real-time.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
