from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Test Coverage Heatmap")
text("Visualize code coverage across your codebase with interactive heatmaps.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
