from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Portfolio Analyzer")
text("Upload stock holdings and analyze performance with interactive charts and benchmarks.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
