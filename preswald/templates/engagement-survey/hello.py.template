from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Employee Engagement Survey")
text("Create and analyze employee engagement surveys with interactive results.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
