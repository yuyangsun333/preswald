from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Interactive Product Requirement Docs")
text("Create dynamic product requirements with user stories and acceptance criteria.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
