from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Incident Postmortem Report Generator")
text("Document incidents, root causes, and action items with a structured template.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
