from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Release Notes Composer")
text("Organize PRs and issues into structured release notes with changelog formatting.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
