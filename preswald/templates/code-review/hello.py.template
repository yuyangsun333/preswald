from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Code Review Dashboard")
text("Track code review metrics, PR turnaround times, and reviewer activity.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
