from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Event Planning Dashboard")
text("Manage event logistics, attendee tracking, and budget monitoring.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
