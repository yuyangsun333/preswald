from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Team OKR Tracker")
text("Monitor team objectives and key results with progress tracking and alignment metrics.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
