from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Sales Territory Planning")
text("Optimize sales territories with market potential analysis and resource allocation.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
