from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Crypto Holdings Tracker")
text("Monitor cryptocurrency investments with real-time price tracking and portfolio analysis.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
