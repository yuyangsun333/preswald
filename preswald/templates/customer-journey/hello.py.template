from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Customer Journey Analytics")
text("Analyze customer touchpoints, conversion paths, and journey optimization opportunities.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
