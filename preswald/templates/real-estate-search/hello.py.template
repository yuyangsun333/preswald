from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Real Estate Search Dashboard")
text("Compare MLS listings and analyze market trends with interactive visualizations.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
