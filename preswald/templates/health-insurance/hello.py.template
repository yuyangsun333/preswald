from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Health Insurance Comparison Tool")
text("Compare health insurance plans, coverage, and costs with interactive visualizations.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
