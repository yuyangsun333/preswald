from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Vendor Management Dashboard")
text("Track vendor performance, contracts, and relationship metrics.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
