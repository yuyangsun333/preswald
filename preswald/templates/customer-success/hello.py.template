from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Customer Success Scorecard")
text("Track customer health, engagement, and success metrics across your portfolio.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
