from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Visualize Math Concepts")
text("Create interactive mathematical visualizations and concept demonstrations.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
