from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Podcast Analytics Dashboard")
text("Track episode performance, listener demographics, and engagement metrics.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
