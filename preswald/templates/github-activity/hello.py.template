from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# GitHub Activity Dashboard")
text("Visualize commit history, PR activity, and team contributions across repositories.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
