from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Bacteria Evolution Simulation")
text("Visualize bacterial growth and evolution patterns with interactive simulations.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
