from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# User Retention Cohorts")
text("Analyze user retention patterns with cohort analysis and visualization tools.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
