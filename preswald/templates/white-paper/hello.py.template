from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Industry White Paper")
text("Create professional white papers with data visualization and industry insights.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
