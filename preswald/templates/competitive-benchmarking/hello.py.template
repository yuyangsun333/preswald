from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Competitive Benchmarking")
text("Compare competitor products, features, and market positioning with interactive analysis.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
