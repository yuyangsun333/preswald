from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Supply Chain Analytics")
text("Monitor supply chain performance, inventory levels, and logistics metrics.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
