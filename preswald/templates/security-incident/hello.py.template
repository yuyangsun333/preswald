from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Security Incident Response")
text("Document and track security incidents with response protocols and resolution tracking.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
