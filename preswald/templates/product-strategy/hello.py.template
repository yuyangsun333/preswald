from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Product Strategy Whitepaper")
text("Document product strategy with market analysis, competitive positioning, and roadmap.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
