from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Content Calendar Planner")
text("Plan and schedule content across multiple channels with editorial calendar and analytics.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
