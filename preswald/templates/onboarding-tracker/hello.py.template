from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Employee Onboarding Tracker")
text("Manage new hire onboarding with task tracking and progress monitoring.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
