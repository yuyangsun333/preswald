from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Personal Health Metrics Dashboard")
text("Track daily activity, sleep, nutrition, and health indicators with interactive visualizations.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
