from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Service Dependency Graph")
text("Map service dependencies and visualize metrics across your infrastructure.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
