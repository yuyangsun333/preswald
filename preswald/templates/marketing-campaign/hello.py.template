from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Marketing Campaign Performance")
text("Analyze campaign metrics, ROI, and channel performance across your marketing efforts.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
