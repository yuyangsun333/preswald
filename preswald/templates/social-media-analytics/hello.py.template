from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Social Media Analytics Dashboard")
text("Track engagement, reach, and performance across all social media platforms in one view.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
