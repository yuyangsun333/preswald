from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Executive Summary Dashboard")
text("Track company-wide KPIs, revenue, burn rate, headcount, and OKR progress in one comprehensive view. 📊")

# Load the CSV
connect()
df = get_df('sample_csv')

# Display key metrics in a table
text("## Key Metrics")
table(df)