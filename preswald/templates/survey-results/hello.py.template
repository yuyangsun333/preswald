from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Visualize Survey Results")
text("Create interactive visualizations for survey data with charts and insights.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
