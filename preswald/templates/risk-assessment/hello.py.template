from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Risk Assessment Matrix")
text("Evaluate and track business risks with probability and impact analysis.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
