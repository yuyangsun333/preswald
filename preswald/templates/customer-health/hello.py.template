from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Customer Health Dashboard")
text("Monitor customer engagement, satisfaction, and risk factors in real-time.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
