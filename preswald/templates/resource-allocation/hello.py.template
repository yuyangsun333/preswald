from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Team Resource Allocation")
text("Optimize team resources with workload balancing and capacity planning.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
