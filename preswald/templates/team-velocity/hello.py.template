from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Team Velocity Tracker")
text("Monitor sprint velocity, story points, and team performance metrics.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
