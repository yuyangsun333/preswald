from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# SEO Performance Tracker")
text("Monitor keyword rankings, backlinks, and organic traffic with detailed analytics.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
