from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Burn Rate & Runway Dashboard")
text("Monitor cash burn, runway projections, and financial health metrics in real-time.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
