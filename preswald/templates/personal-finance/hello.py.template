from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Personal Finance Dashboard")
text("Track expenses, investments, and financial goals in a Mint-like interface.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
