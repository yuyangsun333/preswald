from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# On-call Handoff Report")
text("Create comprehensive handoff reports for on-call rotations with key metrics and alerts.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
