from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Personal Resume Website")
text("Create a professional portfolio website with interactive sections and project showcases.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
