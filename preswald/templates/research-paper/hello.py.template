from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Research Paper")
text("Format academic papers with proper citations, figures, and references.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
