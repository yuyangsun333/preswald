from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Project Budget Tracker")
text("Monitor project expenses, budget allocation, and cost projections.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
