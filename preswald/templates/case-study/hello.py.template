from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Interactive Case Study")
text("Create engaging case studies with interactive elements and data visualization.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
