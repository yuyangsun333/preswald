from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Team Retrospective Dashboard")
text("Facilitate team retrospectives with action item tracking, sentiment analysis, and improvement metrics.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
