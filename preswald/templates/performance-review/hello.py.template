from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Performance Review Template")
text("Structure employee performance reviews with goals, achievements, and development plans.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
