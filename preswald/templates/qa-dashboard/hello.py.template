from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Quality Assurance Dashboard")
text("Track test coverage, bug metrics, and quality assurance KPIs.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
