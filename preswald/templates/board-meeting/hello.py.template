from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Board Meeting Deck")
text("Present quarterly financials, strategic goals, key initiatives, and hiring plans to your board.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
