from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Mortgage Refinancing Dashboard")
text("Compare mortgage options and calculate refinancing scenarios with interactive tools.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
