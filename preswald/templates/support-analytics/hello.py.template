from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Customer Support Analytics")
text("Track support ticket metrics, response times, and customer satisfaction scores.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
