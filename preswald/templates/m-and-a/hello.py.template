from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# M&A Due Diligence Memo")
text("Document M&A due diligence findings with financial analysis and risk assessment.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
