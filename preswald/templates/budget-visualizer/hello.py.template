from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Personal Budget Visualizer")
text("Track spending patterns and budget categories with interactive charts and analysis.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
