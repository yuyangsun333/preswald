from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Sales Pipeline Dashboard")
text("Track deals, forecast revenue, and analyze sales performance across your pipeline.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
