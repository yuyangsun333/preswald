from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Market Research Analysis")
text("Analyze market trends, competitor data, and industry insights with interactive visualizations.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
