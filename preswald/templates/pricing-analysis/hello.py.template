from preswald import text, plotly, connect, get_df, table
import pandas as pd
import plotly.express as px

text("# Product Pricing Analysis")
text("Analyze pricing strategies, competitor pricing, and market positioning.")

# Load the CSV
connect()
df = get_df('sample_csv')

# Show the data
table(df)
